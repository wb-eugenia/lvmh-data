version: '3.8'

services:
  lvmh-app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - MISTRAL_API_KEY=${MISTRAL_API_KEY}
    volumes:
      - ./output:/app/output
      - ./lvmh_store.db:/app/lvmh_store.db
