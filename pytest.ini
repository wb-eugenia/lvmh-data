[pytest]
addopts = -ra
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
norecursedirs = archive scripts
markers =
    integration: tests that require external services or API keys
    slow: slow tests
